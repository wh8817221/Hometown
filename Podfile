# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'Hometown' do
  use_frameworks!
  pod 'Alamofire', '~> 4.6.0'
  pod 'Kingfisher', '~> 4.5.0'
  pod 'ObjectMapper', '~> 3.1.0'
  
  pod 'PKHUD', '~> 5.0.0'
  pod 'SnapKit', '~> 4.0.0'
  pod 'DynamicColor', '~> 4.0.2'
  pod 'PhoneNumberKit', '~> 2.1.0'
  
  target 'HometownTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'HometownUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
